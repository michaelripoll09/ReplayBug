# Dependencies
node_modules/
.pnp
.pnp.*

# Build outputs
dist/
build/
out/
.next/
.turbo/
*.tsbuildinfo

# Vite
apps/demo/dist/

# Test outputs
coverage/
.nyc_output/
test-results/
playwright-report/

# E2E runtime fixtures (contain per-run DSNs, never commit)
.e2e/

# Environment
.env
.env.local
.env.*.local

# Drizzle artifacts (keep migrations, ignore local db state)
*.db
*.sqlite

# Local artifact storage (future release artifacts live outside the repo)
.artifacts/
data/

# Agent/tool local state (auto-generated, never commit)
.atl/

# Local planning and implementation records (keep locally, never publish)











# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/*
!.vscode/extensions.json
