{
  "name": "@replaybug/cli",
  "version": "0.1.0",
  "description": "ReplayBug command-line interface (foundation: --version and --help only)",
  "license": "MIT",
  "type": "module",
  "bin": {
    "replaybug": "./bin/replaybug.js"
  },
  "files": [
    "bin"
  ],
  "scripts": {
    "build": "echo \"@replaybug/cli: no build step (plain Node bin)\"",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@replaybug/config": "workspace:*",
    "@types/node": "22.20.3",
    "typescript": "5.9.3",
    "vitest": "5.0.1"
  }
}
