-- ReplayBug Drizzle migrations directory.
-- No domain tables exist yet (foundation bootstrap only). Later blocks add
-- forward-only migrations here via `pnpm --filter @replaybug/db db:generate`.
SELECT 1;
