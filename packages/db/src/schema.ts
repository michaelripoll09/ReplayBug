/**
 * Placeholder Drizzle schema module.
 *
 * No domain tables exist yet. This module intentionally exports an empty
 * schema map so drizzle-kit has a stable entrypoint while later blocks add
 * workspaces, projects, events, issues and related tables via forward-only
 * migrations (never by editing already-released migrations).
 */
export const schema = {} as const;
