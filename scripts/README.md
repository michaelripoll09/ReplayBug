# Scripts

Operational Node scripts live here. They must stay cross-platform
(Windows PowerShell 5.1 compatible): prefer Node scripts over Unix-only
shell scripts.

## Current state (foundation)

No operational scripts exist yet. Later blocks will add, at minimum:

- seed scripts for the synthetic demo workspace,
- ingest benchmark helpers,
- generated-test verification helpers.

See `` section 8 for the planned files.
